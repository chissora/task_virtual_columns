# task_virtual_columns

Tiny utility to add a computed column to a DataFrame from a simple arithmetic rule referencing existing columns. Supports only addition (+), subtraction (-), and multiplication (*). Column names must contain only letters and underscores. Expressions are evaluated strictly left-to-right

Implemented and tested in Python 3.11.4